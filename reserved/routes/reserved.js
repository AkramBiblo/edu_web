let sql = `UPDATE allStudents SET nameBangla = "${nameBangla}", previousClass = "${previousClass}", gradePoint = "${gradePoint}",
      previousInstitute = "${previousInstitute}", dateOfBirth = "${dateOfBirth}", bloodGroup = "${bloodGroup}",
      residentialService = "${residentialService}", transportService = "${transportService}", deasesQuery = "${deasesQuery}",
      deasesDetailes = "${deasesDetailes}", fNameBangla = "${fNameBangla}", fNID = "${fNID}", fOccupation = "${fOccupation}",
      fMonthlyIncome = "${fMonthlyIncome}", fBloodGroup = "${fBloodGroup}", fEQualification = "${fEQualification}", fMobile = "${fMobile}",
      mNameBangla = "${mNameBangla}", mNameEnglish = "${mNameEnglish}", mNID = "${mNID}", mOccupation = "${mOccupation}",
      mMonthlyIncome = "${mMonthlyIncome}", mBloodGroup = "${mBloodGroup}", mEQualification = "${mEQualification}", mMobile = "${mMobile}",
      vlg = "${vlg}", po = "${po}", ps = "${ps}", dist = "${dist}", addressAreSame = "${addSame}",  pvlg = "${pvlg}", ppo = "${ppo}",
      pps = "${pps}", pdist = "${pdist}", password = "${password}", editButton = '' WHERE sid = "${sid}"`;



      gradePoint = "${gradePoint}"

      let sql = `UPDATE allstudents SET nameBangla = "${nameBangla}", previousClass = "${previousClass}", gradePoint = "${gradePoint}", previousInstitute = "${previousInstitute}", vlg = "${vlg}", po = "${po}", ps = "${ps}", dist = "${dist}", pvlg = "${pvlg}", ppo = "${ppo}", pps = "${pps}", pdist = "${pdist}" WHERE sid = "${sid}"`;